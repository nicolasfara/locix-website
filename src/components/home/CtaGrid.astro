<section class="cta-grid home-grid not-content">
  <a class="cta-grid__link home-card home-card--quiet" href="/research/">
    <strong class="cta-grid__title">Research</strong>
    <span class="cta-grid__copy">Read the unifying claim, terminology bridge, and dissemination-ready summary material.</span>
  </a>
  <a class="cta-grid__link home-card home-card--quiet" href="/evidence/">
    <strong class="cta-grid__title">Evidence</strong>
    <span class="cta-grid__copy">See which public claims come from the paper, the repo, and the integrated case studies.</span>
  </a>
  <a class="cta-grid__link home-card home-card--quiet" href="/examples/">
    <strong class="cta-grid__title">Examples</strong>
    <span class="cta-grid__copy">Jump into the runnable Scala examples and the mixed-paradigm scenarios worth showing first.</span>
  </a>
</section>

<style>
  .cta-grid {
    margin-top: 2rem;
    grid-template-columns: repeat(auto-fit, minmax(15rem, 1fr));
  }

  .cta-grid__link {
    display: flex;
    flex-direction: column;
    gap: 0.45rem;
    color: inherit;
    text-decoration: none;
    transition: transform 180ms ease, border-color 180ms ease, box-shadow 180ms ease;
  }

  .cta-grid__link:hover {
    transform: translateY(-2px);
    border-color: color-mix(in srgb, var(--sl-color-text-accent) 40%, transparent);
    box-shadow: 0 18px 30px -28px color-mix(in srgb, black 46%, transparent);
  }

  .cta-grid__link:focus-visible {
    outline: 2px solid color-mix(in srgb, var(--sl-color-text-accent) 55%, transparent);
    outline-offset: 3px;
  }

  .cta-grid__title {
    font-size: 1.08rem;
    line-height: 1.2;
    letter-spacing: -0.02em;
  }

  .cta-grid__copy {
    color: var(--sl-color-gray-2);
    line-height: 1.45;
  }
</style>
