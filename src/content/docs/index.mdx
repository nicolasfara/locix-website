---
title: Home
description: A capabilities-and-placement-types approach to unifying multitier, choreographic, and aggregate programming in one typed distributed model.
template: splash
hero:
  title: |
    <span class="block max-w-[18ch] text-[clamp(2.5rem,2vw,5.5rem)] leading-[1.0] font-black tracking-[-0.045em]">
      <span class="brand-name brand-name--hero" aria-label="CaMiL"><span aria-hidden="true"><span>C</span><span>a</span><span>M</span><span>i</span><span>L</span></span></span>
      Multiparty Programming for <span class="hero-theme-accent">modern</span> and <span class="hero-theme-accent">safe</span> distributed systems development.
    </span>
  tagline: |
    <span class="block max-w-3xl text-base leading-7 text-[var(--sl-color-gray-2)] sm:text-lg">
      A Scala framework for multiparty computation,
      <span class="font-semibold text-[var(--sl-color-white)] decoration-[var(--sl-color-text-accent)] underline underline-offset-4">safe by design</span>
      through capabilities and placement types across
      <span class="italic font-semibold text-[var(--sl-color-text-accent)]">multitier</span>,
      <span class="italic font-semibold text-[var(--sl-color-text-accent)]">choreography</span>, and
      <span class="italic font-semibold text-[var(--sl-color-text-accent)]">collective</span>
      programming.
    </span>
  image:
    dark: ../../assets/locix-logo-dark.svg
    light: ../../assets/locix-logo-light.svg
    alt: Framework logo
  actions:
    - text: View on GitHub
      link: https://github.com/nicolasfara/locix
      icon: github
    - text: Read the research
      link: /locix-website/research/
      icon: document
      variant: minimal
---

import EvidenceStrip from '../../components/home/EvidenceStrip.astro';
import Foundations from '../../components/home/Foundations.astro';
import Publications from '../../components/home/Publications.astro';
import SafetyFeatures from '../../components/home/SafetyFeatures.astro';
import BrandName from '../../components/BrandName.astro';

{/* <EvidenceStrip /> */}

## A Shared Foundation

<BrandName /> brings **architectural placement**, **protocol coordination**, and *neighborhood-style collective behavior* together in **one cohesive model**.
This framework lets you combine these styles naturally instead of treating them as separate approaches with separate abstractions.
By building on **capabilities** and **placement types**, <BrandName /> helps you compose them naturally while keeping boundaries **clear**, interactions **safe**, and the overall design *easier to reason about*.
<Foundations />

## Safety Features

<BrandName /> keeps **placement explicit**, **capabilities scoped**, and **cross-paradigm handoffs typed**, so composition preserves clear boundaries instead of weakening guarantees.
That means values remain attached to the peers that own them, paradigm-specific operations are only available inside the right context, and data or behavior **cannot silently cross locations** without an explicit typed step.
Instead of discovering mistakes later as *protocol bugs*, *invalid captures*, or *misplaced computations* at runtime, you get **direct feedback from the compiler** at the exact point where the boundary is being violated.
Scala's type system and capture checking make these guarantees practical to express and enforce at compile time, letting you combine **multitier**, **choreographic**, and **collective** code with stronger safety and *far less defensive boilerplate*.

<SafetyFeatures />

# Publication

<Publications />
